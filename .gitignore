# ----- Secrets -----
.env
*.local

# ----- Node / SvelteKit (app/) -----
node_modules/
app/.svelte-kit/
app/build/
app/.vite/
app/dist/
.DS_Store

# ----- Generierte Kampagnen-Historie -----
# WICHTIG: campaign/ selbst wird getrackt (das sind deine Daten!),
# nur die generierte Historie nicht.
campaign/.history/
